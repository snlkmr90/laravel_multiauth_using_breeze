## About Laravel Multi Auth Using Breeze
    Project is create using laravel/breeze package.
        -composer require laravel/breeze
        -php artisan breeze:install
        -npm install
        -npm run dev